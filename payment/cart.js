\ndocument.addEventListener(\'DOMContentLoaded\', () => {\n    // The Supabase client is loaded from supabase.js\n    if (window.supabase) {\n        // You may not need to load all products on the cart page,\n        // but this is here as a placeholder.\n        // loadProducts(); \n    } else {\n        console.error(\'Supabase client not found. Make sure supabase.js is loaded before script.js.\');\n    }\n    \n    const cartContainer = document.querySelector(\'.col-25 .container\');\n    if (cartContainer) {\n        displayCartSummary();\n    }\n});\n\nfunction displayCartSummary() {\n    const cart = JSON.parse(localStorage.getItem(\'cart\')) || [];\n    const cartContainer = document.querySelector(\'.col-25 .container\');\n    \n    if (cartContainer) {\n        let totalQuantity = 0;\n        let totalPrice = 0;\n        let cartSummaryHTML = \'<h4>Cart <span class=\"price\" style=\"color:black\"><i class=\"fa fa-shopping-cart\"></i> <b>\';\n        \n        const cartItemsHTML = cart.map(item => {\n            totalQuantity += item.quantity;\n            totalPrice += item.price * item.quantity;\n            return `<p><a href=\"#\">${item.name} (x${item.quantity})</a> <span class=\"price\">$${(item.price * item.quantity).toFixed(2)}</span></p>`;\n        }).join(\'\');\n        \n        cartSummaryHTML += `${totalQuantity}</b></span></h4>\`;\n        cartSummaryHTML += cartItemsHTML;\n        cartSummaryHTML += `<hr><p>Total <span class=\"price\" style=\"color:black\"><b>$${totalPrice.toFixed(2)}</b></span></p>`;\n        \n        cartContainer.innerHTML = cartSummaryHTML;\n    }\n}\n